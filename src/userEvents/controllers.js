const jwt = require("jsonwebtoken");
const UserEvents = require("./model");

// CRUD

// Create - at login ?

// Read - for profile

// Update - change status going / maybe / not going

// Delete - status not going ?
